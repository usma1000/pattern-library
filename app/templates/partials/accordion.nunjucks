<h2 class="pattern-header" id="accordion">Accordion</h2>
<section class="pattern-section">
	<p class="pattern-text">The <code>&lt;section></code> tag can be replaced with a <code>&lt;div></code>.</p>
	<div class="display-box">
		{% include 'partials/mod/mod_accordion.html' %}
	</div>
	{% import 'partials/showcode.nunjucks' as showcode %}
	{{ showcode.showmarkup("mod_accordion") }}
	{{ showcode.showcss("mod_accordion") }}
	{{ showcode.showjs("mod_accordion") }}
</section>